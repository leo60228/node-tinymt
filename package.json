{
  "name": "node-tinymt",
  "version": "1.0.0",
  "description": "A node.js wrapper for TinyMT",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install": "node-gyp configure && node-gyp build"
  },
  "author": "leo60228",
  "license": "BSD-3-Clause",
  "dependencies": {
    "bindings": "^1.2.1"
  }
}
